import { Typography } from '@mui/material';
import { Box } from '@mui/system';
import React from 'react';

const Pay = () => {
    return (
        <Box style={{background: "#AD0C0C", widht: '100vw', height: '80vh', display: 'flex', justifyContent: 'center', alignItems: 'center', color: 'white'}}>
            <Typography variant="h2">Payment method coming soon...</Typography>
        </Box>
    );
};

export default Pay;